
import 'package:flutter/material.dart';

class BeanManagementPage extends StatelessWidget {
  const BeanManagementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Bean");
  }
}